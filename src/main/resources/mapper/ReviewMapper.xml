<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acgm.acgmmediatracker.mapper.ReviewMapper">

    <resultMap id="ReviewResultMap" type="com.acgm.acgmmediatracker.entity.Review">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="mediaId" column="media_id" />
        <result property="rating" column="rating" />
        <result property="content" column="content" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <sql id="Review_Columns">
        id,
        user_id,
        media_id,
        rating,
        content,
        created_at
    </sql>

    <select id="selectById" resultMap="ReviewResultMap">
        SELECT <include refid="Review_Columns" />
        FROM reviews
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="ReviewResultMap">
        SELECT <include refid="Review_Columns" />
        FROM reviews
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.acgm.acgmmediatracker.entity.Review">
        INSERT INTO reviews (
            user_id,
            media_id,
            rating,
            content,
            created_at
        ) VALUES (
            #{userId},
            #{mediaId},
            #{rating},
            #{content},
            #{createdAt}
        )
    </insert>

    <update id="update" parameterType="com.acgm.acgmmediatracker.entity.Review">
        UPDATE reviews
        SET
            user_id = #{userId},
            media_id = #{mediaId},
            rating = #{rating},
            content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM reviews
        WHERE id = #{id}
    </delete>

</mapper>
